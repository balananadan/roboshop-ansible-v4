[Unit]
Description=RoboShop Cart Service
After=network.target

[Service]
Type=simple
User=appuser
WorkingDirectory=/app
ExecStart=/usr/bin/node server.js
Restart=on-failure
RestartSec=10
SyslogIdentifier=cart

Environment=REDIS_HOST=valkey-{{ env }}.piple.site
Environment=CATALOGUE_URL=http://catalogue-{{ env }}.piple.site:8002
Environment=PORT=8003

[Install]
WantedBy=multi-user.target